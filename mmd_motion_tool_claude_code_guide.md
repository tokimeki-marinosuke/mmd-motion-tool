# MMDモーション生成ツール実装指導書(ルールベース版・Claude Code入門)

## この指導書の目的

Claude APIを使わず、ルールベース(キーワード辞書・条件分岐)で「文章→モーション選択・調整→VMD出力」を実現するプロトタイプを、Claude Codeを使って段階的に実装するための手順書である。Claude Codeを初めて使う前提で、使い方の基本から説明する。

---

## 1. なぜ「一度に全部指示する」のがよくないのか

結論から言うと、一度に大量の指示を出すのは避けるべきである。理由は以下の通り。

- **動作確認ができないまま進んでしまう**。VMD入出力、ボーン知識、モーション検索、強度調整をまとめて指示すると、どこかにバグがあっても最後まで気づけない。
- **Claude Codeが仕様を勝手に解釈して補ってしまう**。曖昧な部分は、Claude Codeが「たぶんこうだろう」と判断して実装するため、意図とズレたまま大量のコードが生成されるリスクがある。
- **レビューの負担が大きい**。一度に1000行生成されると、全部読んで理解するのが困難になり、結果的に「動いているから良し」で済ませてしまいがちになる。それは技術的負債になる。
- **後戻りのコストが高い**。土台(VMD入出力)が間違っていると、その上に積み上げた機能を全部作り直すことになる。

そのため、**小さく確実な単位(フェーズ)に分割し、各フェーズごとに「実装→動作確認→次へ」を繰り返す**方針を取る。

---

## 2. Claude Codeの基本的な使い方

### 2.1 起動方法

ターミナルでプロジェクトディレクトリに移動し、以下を実行する。

```bash
cd ~/projects/mmd-motion-tool
claude
```

対話型のセッションが起動し、そこに指示を日本語で入力すればよい。

### 2.2 CLAUDE.mdファイルを作る

プロジェクトのルートに `CLAUDE.md` というファイルを置くと、Claude Codeがセッションのたびに自動的に読み込み、プロジェクトの背景・方針・規約を覚えた状態で作業してくれる。**これは最初に必ず作るべきである。**

`CLAUDE.md` に書く内容の例:

```markdown
# プロジェクト概要

MMD向けのAIモーション生成・編集ツール。文章による指示(例:「元気よく手を振る」)から、
MMDで使えるVMDモーションファイルを生成する。まずはAPIを使わないルールベース版を作る。

## 方針
- Python 3.11を使用する
- 外部ライブラリは最小限にする(標準ライブラリ優先)
- VMDバイナリ形式の仕様は docs/vmd_format.md にまとめる
- 各機能は独立したモジュールとして実装し、テストを書く
- コードコメントは日本語で書く

## ディレクトリ構成
- src/vmd/       VMD入出力
- src/bones/     ボーン・モーフ知識層
- src/rules/     ルールベースの自然言語解析
- src/motions/   モーション素材とタグ検索
- tests/         テストコード
- sample_motions/ 検証用の手作りVMDサンプル
```

### 2.3 指示の出し方の基本

- 1回の指示は「1つのフェーズ」「1つの機能」に絞る
- 「〜を実装して」だけでなく、「〜まで実装したらテストコードも書いて、実行結果を見せて」と伝えると、Claude Code自身が検証まで行ってくれる
- 生成されたコードは必ず自分の目で軽く確認する。わからない部分は「この部分は何をしているか説明して」と聞けばよい
- うまくいったら `git commit` するようClaude Codeに頼む、あるいは自分でコミットする。フェーズごとにコミットしておくと、後で戻れて安心

### 2.4 セッションが長くなってきたら

1つのセッション内で会話が長くなると、Claude Codeが古いやり取りを忘れたり、コンテキストが圧迫されたりすることがある。フェーズが1つ終わったタイミングで、一度セッションを終了し(`/exit` や新しいターミナルで再度 `claude`)、新しいセッションで次のフェーズを指示するのがおすすめである。`CLAUDE.md` があるので、プロジェクトの前提は毎回引き継がれる。

---

## 3. 実装フェーズ全体像

| フェーズ | 内容 | 目安期間 |
|---|---|---|
| 0 | プロジェクトセットアップ | 最初の30分 |
| 1 | VMD入出力(パーサー/ライター) | 1〜2セッション |
| 2 | ボーン・モーフ知識層 | 1セッション |
| 3 | サンプルモーション素材の準備とタグ付け | 1セッション |
| 4 | ルールベース自然言語解析 | 1〜2セッション |
| 5 | 検索→VMD出力の最小ループ統合 | 1セッション |
| 6 | 強度調整(振り幅・速度スケーリング) | 1セッション |
| 7 | CLIとしてまとめる | 1セッション |

各フェーズの指示例を以下に示す。**そのままコピーして使ってよい。**

---

## 4. フェーズ別の指示内容

### フェーズ0: プロジェクトセットアップ

```
Pythonプロジェクトのひな形を作ってほしい。プロジェクト名はmmd-motion-tool。
以下の構成にしたい:
- src/vmd/
- src/bones/
- src/rules/
- src/motions/
- tests/
- sample_motions/
- CLAUDE.md(このプロジェクトの概要と方針を書いたもの)
- README.md
- pyproject.toml (pytestを使えるように)

CLAUDE.mdの内容は、私がこの後説明する概要をもとに書いてほしい。
[ここに1章の目的や2.2節の内容を貼り付けて伝える]
```

完了確認: `pytest` が空のテストでも動くこと、ディレクトリ構成ができていることを確認する。

---

### フェーズ1: VMD入出力

VMDはバイナリ形式なので、ここが一番技術的に重要な土台になる。**このフェーズは特に、一気に「読み込みも書き込みも全部」と頼まず、読み込み→書き込みの順で分けるとよい。**

**1-a: 仕様調査とドキュメント化**

```
VMDファイルフォーマット(MikuMikuDance用モーションデータ形式)のバイナリ仕様を
調べて、docs/vmd_format.md にまとめてほしい。含めるべき内容は、ヘッダー、
ボーンフレーム、モーフ(表情)フレーム、カメラフレーム、照明フレーム、
セルフ影フレーム、IKフレームそれぞれの構造(オフセット、バイト長、
エンコーディング)。ウェブ検索を使って正確な情報を集めてよい。
```

**1-b: パーサー実装**

```
docs/vmd_format.md の仕様に基づいて、src/vmd/parser.py にVMDファイルを
読み込んでPythonのデータクラスに変換する機能を実装してほしい。
ボーンフレームとモーフフレームの読み込みだけで、まずは十分。
カメラ・照明・セルフ影・IKフレームは後回しでよい。
テストコードも書いて、パース結果をprintして見せてほしい。
```

**1-c: サンプルVMDでの検証**

MMD配布サイトなどから短い挙手モーションVMDを1つ手元に用意し(著作権・配布条件を確認の上)、それを読み込ませて実際に構造が正しく取れているか確認する。

**1-d: ライター実装**

```
src/vmd/writer.py に、1-bで定義したデータクラスからVMDバイナリファイルを
書き出す機能を実装してほしい。1-cで読み込んだVMDを、そのまま書き出して
MMD上で元通り再生できるかを検証したいので、読み込み→書き出しの
往復テスト(ラウンドトリップテスト)も書いてほしい。
```

完了確認: **実際にMMDで読み込ませて、意図通り再生されるかを目視確認する。** ここが崩れていると以降すべてが無意味になるので、面倒でも必ず確認する。

---

### フェーズ2: ボーン・モーフ知識層

```
src/bones/mapping.py に、MMD標準ボーン構造の定義をJSONまたはPythonの
辞書として持たせてほしい。センター、上半身、下半身、首、頭、左腕、左ひじ、
左手首、右腕、右ひじ、右手首、左足、左ひざ、左足首、左足ID、左つま先ID、
(右も同様)を含む標準的なボーン名の一覧と、それぞれの親子関係。
表情モーフについても、まばたき、笑い、困る、あ、い、う、え、おなど
標準的なモーフ名の一覧を src/bones/morphs.py にまとめてほしい。
```

---

### フェーズ3: サンプルモーション素材の準備

```
sample_motions/ に、動作タイプ・感情・強度のタグをつけたモーション素材の
メタデータ管理の仕組みを作ってほしい。src/motions/library.py に、
モーションファイルのパスとタグ(action, emotion, intensityなど)を
対応付けるカタログクラスを実装してほしい。
最初は実際のVMDファイルがなくてもよいので、ダミーのメタデータ数件
(例:wave/happy、bow/polite、turn_back/shy)で動作するようにして、
テストで確認してほしい。
```

このフェーズと並行して、まりのすけ自身で数本の短いモーションVMD(挙手、お辞儀、振り返りなど)を用意し、`sample_motions/` に配置するとよい。自作 or 配布モーション(利用規約要確認)のどちらでもよい。

---

### フェーズ4: ルールベース自然言語解析

```
src/rules/parser.py に、日本語の文章から動作タグ・感情タグ・強度を
抽出するルールベースの解析器を実装してほしい。
形態素解析にはMeCabまたはjanomeを使ってよい(pip install可能なものを
提案してほしい)。「元気よく手を振る」のような文から、
{action: "wave", emotion: "happy", intensity: 0.8} のような辞書を
出力する。キーワード辞書(動作キーワード→action、感情キーワード→emotion、
「もっと」「少し」などの強度表現→intensity)は
src/rules/keywords.py に分離してほしい。テストで5〜10種類の
入力文について正しく解析できるか確認してほしい。
```

---

### フェーズ5: 検索→VMD出力の最小ループ

```
これまで作ったsrc/rules(自然言語解析)、src/motions(モーション検索)、
src/vmd(VMD入出力)を組み合わせて、以下の流れをつなげるスクリプトを
src/main.py に実装してほしい。
1. コマンドライン引数で文章を受け取る
2. ルールベース解析でタグを抽出
3. モーションライブラリからタグに最も近いモーションを検索
4. 該当するVMDファイルをそのまま output/ にコピー(このフェーズでは
   編集はしない)
実際に「元気よく手を振って」と入力して、対応するVMDが出力されることを
確認してほしい。
```

完了確認: 出力されたVMDをMMDに読み込ませて再生確認する。ここまでできれば第一段階のMVPが完成する。

---

### フェーズ6: 強度調整

```
src/motions/adjust.py に、VMDのボーンフレームの回転量・移動量に
スケール係数をかけて、動きの大きさを調整する機能を実装してほしい。
「動きを大きく」→ intensityを1.2倍、「控えめに」→ 0.7倍、のような
対応をsrc/rules/keywords.pyの強度辞書と連携させてほしい。
フェーズ5のパイプラインに組み込んで、「大きく手を振って」と
「控えめに手を振って」で出力VMDのボーン回転量が変わることをテストで
確認してほしい。
```

---

### フェーズ7: CLIとしてまとめる

```
これまでの機能をargparseまたはclickを使ったCLIツールとしてまとめてほしい。
`mmd-motion-tool generate "元気よく手を振る" --output output/wave.vmd`
のように使えるようにし、README.mdに使い方を追記してほしい。
```

---

## 5. 各フェーズ共通の進め方チェックリスト

1. 前のフェーズのコミットが残っている状態から始める
2. 上記の指示文をそのまま、または自分の言葉に直して伝える
3. Claude Codeが実装したら、テスト結果を見せてもらう
4. わからない箇所は都度質問する(「なぜこの実装にしたのか」「他の方法はないか」)
5. 問題なければコミットしてから次のフェーズへ進む
6. セッションが長くなったら一度区切って新しいセッションで再開する

---

## 6. 補足:困ったときの聞き方

- 「このエラーの原因を説明して」→ 単に直させるだけでなく理解を優先する
- 「この関数のこの部分が何をしているか、コメントを増やして」
- 「別の実装方針も考えられるか、比較して教えて」
- 「テストが通っているか実行して結果を見せて」

Claude Codeは指示が具体的であるほど精度が上がるので、曖昧な依頼(「いい感じにして」)よりも、本指導書のような「何を、どこに、どういう入出力で」を明示した指示が有効である。
